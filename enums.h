#ifndef ENUMS
#define ENUMS

//############ENUMS################
enum Nroots {
  ZERO_ROOT = 0,
  ONE_ROOT = 1,
  TWO_ROOT = 2,
  INF_ROOT = 8,
};

enum Errors {
  INPUT_ERROR = -1,
  EMPTY_INPUT = -2,
  NO_ERROR = -3
};

#endif
